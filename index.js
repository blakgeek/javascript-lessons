const axios = require('axios');

// returns the first value that is considered false
function getFirstNonFalsyValue(v1, v2, v3, v4) {

}

// makes a GET request to a url passed in as a parameter
// returns the body of the response
function callApi() {

}

// makes a POST request to a url passed in as a parameter an sends the data passed as parameter
// returns the body of the response
function postToApi(url, body) {

}

// returns an object with the following properties
// name = 'Saf',
// city = 'Atlanta',
// stuff = array with the numbers 1,5,99
// doIt = a function that returns "it"
function getAnObject() {

}

module.exports = {
    getFirstNonFalsyValue,
    callApi,
    postToApi,
    getAnObject
};